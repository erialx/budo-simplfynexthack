# External components

This directory is intentionally empty in Git. On the development machine it
contains symlinks to external Orca/MJLab repositories outside this workspace.
Those absolute symlinks are not portable and therefore are ignored.

See the pinned versions and installation instructions in
[`../README.md`](../README.md). Clone the required repositories here or recreate
the symlinks after cloning the private parent repository.

